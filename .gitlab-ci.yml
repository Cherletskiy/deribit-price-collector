image: python:3.12-slim

stages:
  - test

variables:
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONUNBUFFERED: "1"
  LOG_LEVEL: "INFO"

test:
  stage: test
  before_script:
    - python -V
    - pip install uv
    - uv sync --group dev
  script:
    - make check
